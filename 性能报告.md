# Task233 性能报告

最后更新：2026-06-28

本报告记录 Task233 与 UniTask 的 Unity 2022.3.51f1 实测对比。当前口径：不做 warmup，单次 measurement，大循环记录总耗时，再倒推 `ns/op` 与 `ops/s`。本轮完整 EditMode 套件总耗时 23.86 秒。

## 测试环境

| 项目 | 值 |
| --- | --- |
| Unity | 2022.3.51f1 |
| 平台 | WindowsEditor / EditMode |
| Graphics API | Null (`-nographics`) |
| Scripting Backend | Mono2x |
| CPU | AMD Ryzen 9 7900X3D 12-Core Processor |
| 线程数 | 24 |
| 内存 | 64535 MB |
| OS | Windows 11 64bit |
| UniTask | 2.5.11 |
| UniTask 获取方式 | 下载 `2.5.11` 源码包并以 `file:` 本地 package 导入，不使用 git clone / UPM git 依赖 |
| 测试结果 | 28 passed / 0 failed / 0 skipped |
| 测试时间 | 2026-06-28 18:25:24-18:25:47 本机时间 |
| 输出文件 | `%TEMP%/Task233PerfRun20s/editmode-results.xml` |

## 测试口径

| 项目 | 口径 |
| --- | --- |
| Warmup | 0 |
| MeasurementCount | 1 |
| Factory 迭代 | 20,000,000 次 |
| Cancel 迭代 | 2,000,000 次 |
| Post 迭代 | 128 次，包含冷启动 PlayerLoop 初始化 |
| 折算 | `ns/op = 总毫秒 * 1,000,000 / 迭代次数`，`ops/s = 迭代次数 / 总秒数` |

## 结论摘要

| 场景 | Task233 ns/op | 对比方 ns/op | 结果 |
| --- | ---: | ---: | --- |
| `Yield()` factory | 1.189 | 1.228 | Task233 约 1.03x 更快 |
| `DelayFrames(1)` factory | 6.380 | 221.354 | Task233 约 34.7x 更快 |
| `DelaySeconds(0.001)` factory | 6.289 | 292.718 | Task233 约 46.5x 更快 |
| `DelayMilliseconds(1)` factory | 6.283 | 356.741 | Task233 约 56.8x 更快 |
| 取消源 create/cancel/dispose | 17.293 | 157.246 | Task233 约 9.1x 更快 |

## 对比明细

| 场景 | Task233 API | 对比 API | 迭代次数 | Task233 总耗时 | 对比总耗时 | Task233 ops/s | 对比 ops/s | Task233 GC median | 对比 GC median |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Yield factory | `T233.Yield()` | `UniTask.Yield()` | 20,000,000 | 23.7782 ms | 24.5565 ms | 841,106,560 | 814,448,313 | 0.00000015 | 0.00000015 |
| 等待 1 帧 factory | `T233.DelayFrames(1)` | `UniTask.DelayFrame(1)` | 20,000,000 | 127.6081 ms | 4427.0891 ms | 156,729,863 | 4,517,641 | 0.00000015 | 1.00000365 |
| 等待秒 factory | `T233.DelaySeconds(0.001d)` | `UniTask.Delay(TimeSpan.FromSeconds(0.001d))` | 20,000,000 | 125.7895 ms | 5854.3646 ms | 158,995,783 | 3,416,255 | 0.00000015 | 1.00000045 |
| 等待毫秒 factory | `T233.DelayMilliseconds(1)` | `UniTask.Delay(1)` | 20,000,000 | 125.6559 ms | 7134.8265 ms | 159,164,830 | 2,803,152 | 0.00000015 | 1.00000070 |
| 取消句柄 | `CreateCancelSource/Cancel/Dispose` | `CancellationTokenSource/Cancel/Dispose` | 2,000,000 | 34.5854 ms | 314.4922 ms | 57,827,870 | 6,359,458 | 0.00000150 | 1.00000600 |

## Task233 单项

| 场景 | 迭代次数 | 总耗时 | ns/op | ops/s | GC median |
| --- | ---: | ---: | ---: | ---: | ---: |
| `T233.Post(cachedAction)` 冷启动入队 | 128 | 1.6039 ms | 12530.469 | 79,805 | 1.171875 |
| `T233.Yield()` factory | 20,000,000 | 26.5640 ms | 1.328 | 752,898,660 | 0.00000015 |
| `T233.DelayFrames(1)` factory | 20,000,000 | 128.7199 ms | 6.436 | 155,376,131 | 0.00000015 |
| `T233.DelaySeconds(0.001d)` factory | 20,000,000 | 257.5409 ms | 12.877 | 77,657,568 | 0.00000015 |
| `T233.DelayMilliseconds(1)` factory | 20,000,000 | 142.5635 ms | 7.128 | 140,288,363 | 0.00000015 |
| `Task233CancelSource` create/cancel/dispose | 2,000,000 | 33.9812 ms | 16.991 | 58,856,073 | 0.00000000 |

说明：`GC median` 是 Unity Performance Testing `Time.GC()` 的原始输出；Unity XML 将单位标为 `Undefineds`，因此只用于同环境横向比较。

## async/await 与业务场景覆盖

| 测试 | 覆盖内容 |
| --- | --- |
| `YieldContinuesOnNextPump` | `await T233.Yield()` continuation |
| `DelayFramesWaitsRequestedPumps` | 帧等待语义 |
| `DelayMillisecondsZeroCompletesSynchronously` | 毫秒 API 和同步完成 |
| `DelaySecondsZeroCompletesSynchronously` | 秒 API 和同步完成 |
| `CancelSourceStopsAwaitedWork` | await 中取消并抛出 `OperationCanceledException` |
| `PostRunsInFifoOrderForBusinessQueue` | 业务队列 FIFO 顺序 |
| `NestedAsyncWorkflowCompletesInOrder` | 嵌套 async/await 工作流 |
| `OwnerCancelPatternPreventsLateUiMutation` | UI/Owner 销毁后取消，避免 late mutation |
| `DebouncePatternKeepsOnlyLastRequest` | 搜索/输入 debounce，只保留最后请求 |
| `TimeoutPatternCancelsSlowWork` | timeout 取消慢任务 |

这组测试还覆盖了 `Cancel(); Dispose();` 立即调用时，挂起 await 仍能正确观察取消；Runtime 通过 cancellation retain/release 延迟回收句柄槽，避免 id 复用破坏旧等待。

## 平台适配目标

Task233 Runtime 不依赖 worker thread、反射生成、动态代码或 unsafe，调度基于 Unity PlayerLoop，目标覆盖 Unity 2022+ 的 Mono / IL2CPP / WebGL 单线程平台。当前 package 是 Unity-first 运行时；取消句柄和 awaitable 形状保持 .NET Standard 友好，后续可拆出传统 C# 通用 scheduler。

## 运行方式

先下载 UniTask 源码包到本地 `.perf/UniTask`：

```powershell
powershell -ExecutionPolicy Bypass -File Tools/PrepareUniTaskPackage.ps1 -Version 2.5.11
```

再运行 Unity 2022.3.51f1：

```powershell
"C:\Program Files\Unity\Hub\Editor\2022.3.51f1\Editor\Unity.exe" -batchmode -nographics -projectPath TestProject -runTests -testPlatform EditMode -testResults artifacts\editmode-results.xml
```

GitHub Actions 会下载 UniTask `2.5.11` 源码包、创建隔离测试工程，并运行同一套测试。仓库没有配置 Unity license secret 时，CI 会通过配置校验并跳过真实 Unity Editor 测试。
