# Task233 性能报告

最后更新：2026-06-28

本报告记录 Task233 与 UniTask 的短时实测对比。当前口径按用户要求调整为：不做 warmup，单次 measurement，大循环记录总耗时，再倒推 `ns/op` 与 `ops/s`。本轮 Unity 测试总耗时 1.36 秒。

## 测试环境

| 项目 | 值 |
| --- | --- |
| Unity | 2022.3.51f1 |
| 平台 | WindowsEditor / EditMode |
| Graphics API | Null (`-nographics`) |
| Scripting Backend | Mono2x |
| CPU | AMD Ryzen 9 7900X3D 12-Core Processor |
| 线程数 | 24 |
| 内存 | 64535 MB |
| OS | Windows 11 64bit |
| UniTask | 2.5.10（本机离线缓存；GitHub 拉取 2.5.11 时网络超时） |
| 测试结果 | 18 passed / 0 failed / 0 skipped |
| 测试时间 | 2026-06-28 17:02:02-17:02:03 本机时间 |
| 输出文件 | `%TEMP%/Task233PerfRun/editmode-results.xml` |

## 测试口径

| 项目 | 口径 |
| --- | --- |
| Warmup | 0 |
| MeasurementCount | 1 |
| Factory 迭代 | 1,000,000 次 |
| Cancel 迭代 | 100,000 次 |
| Post 迭代 | 128 次，包含冷启动 PlayerLoop 初始化 |
| 折算 | `ns/op = 总毫秒 * 1,000,000 / 迭代次数`，`ops/s = 迭代次数 / 总秒数` |

## 结论摘要

| 场景 | Task233 ns/op | 对比方 ns/op | 结果 |
| --- | ---: | ---: | --- |
| `Yield()` factory | 1.249 | 1.254 | Task233 略快，约 1.00x |
| `DelayFrames(1)` factory | 11.769 | 214.967 | Task233 约 18.3x 更快 |
| `DelaySeconds(0.001)` factory | 12.769 | 252.724 | Task233 约 19.8x 更快 |
| `DelayMilliseconds(1)` factory | 12.141 | 258.307 | Task233 约 21.3x 更快 |
| 取消源 create/cancel/dispose | 36.517 | 138.634 | Task233 约 3.8x 更快 |

## 对比明细

| 场景 | Task233 API | 对比 API | 迭代次数 | Task233 总耗时 | 对比总耗时 | Task233 ops/s | 对比 ops/s | Task233 GC median | 对比 GC median |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Yield factory | `T233.Yield()` | `UniTask.Yield()` | 1,000,000 | 1.2488 ms | 1.2535 ms | 800,768,738 | 797,766,254 | 0.000003 | 0.000003 |
| 等待 1 帧 factory | `T233.DelayFrames(1)` | `UniTask.DelayFrame(1)` | 1,000,000 | 11.7693 ms | 214.9674 ms | 84,966,820 | 4,651,868 | 0.000003 | 1.000059 |
| 等待秒 factory | `T233.DelaySeconds(0.001d)` | `UniTask.Delay(TimeSpan.FromSeconds(0.001d))` | 1,000,000 | 12.7694 ms | 252.7236 ms | 78,312,215 | 3,956,892 | 0.000003 | 1.000006 |
| 等待毫秒 factory | `T233.DelayMilliseconds(1)` | `UniTask.Delay(1)` | 1,000,000 | 12.1405 ms | 258.3070 ms | 82,368,930 | 3,871,362 | 0.000003 | 1.000011 |
| 取消句柄 | `CreateCancelSource/Cancel/Dispose` | `CancellationTokenSource/Cancel/Dispose` | 100,000 | 3.6517 ms | 13.8634 ms | 27,384,506 | 7,213,238 | 0.000030 | 1.000030 |

## Task233 单项

| 场景 | 迭代次数 | 总耗时 | ns/op | ops/s | GC median |
| --- | ---: | ---: | ---: | ---: | ---: |
| `T233.Post(cachedAction)` 冷启动入队 | 128 | 1.4437 ms | 11278.906 | 88,661 | 1.171875 |
| `T233.Yield()` factory | 1,000,000 | 1.9781 ms | 1.978 | 505,535,615 | 0.000003 |
| `T233.DelayFrames(1)` factory | 1,000,000 | 11.7085 ms | 11.708 | 85,408,037 | 0.000003 |
| `T233.DelaySeconds(0.001d)` factory | 1,000,000 | 12.3874 ms | 12.387 | 80,727,191 | 0.000003 |
| `T233.DelayMilliseconds(1)` factory | 1,000,000 | 12.3629 ms | 12.363 | 80,887,170 | 0.000003 |
| `Task233CancelSource` create/cancel/dispose | 100,000 | 3.5624 ms | 35.624 | 28,070,963 | 0.000000 |

说明：`GC median` 是 Unity Performance Testing `Time.GC()` 的原始输出；Unity XML 将单位标为 `Undefineds`，因此只用于同环境横向比较。

## 平台适配目标

Task233 Runtime 不依赖 worker thread、反射生成、动态代码或 unsafe，调度基于 Unity PlayerLoop，目标覆盖 Unity 2022+ 的 Mono / IL2CPP / WebGL 单线程平台。当前 package 是 Unity-first 运行时；取消句柄和 awaitable 形状保持 .NET Standard 友好，后续可拆出传统 C# 通用 scheduler。

## 运行方式

本地 Unity 2022.3.51f1：

```powershell
"C:\Program Files\Unity\Hub\Editor\2022.3.51f1\Editor\Unity.exe" -batchmode -nographics -projectPath TestProject -runTests -testPlatform EditMode -testResults artifacts\editmode-results.xml
```

GitHub Actions 会创建隔离测试工程并运行同一套测试。仓库没有配置 Unity license secret 时，CI 会通过配置校验并跳过真实 Unity Editor 测试。
